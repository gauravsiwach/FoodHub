apiVersion: v1
kind: Service
metadata:
  name: foodhub-api
  labels:
    app: foodhub-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: foodhub-api
